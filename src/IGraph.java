public interface IGraph {
    public void addVertex(Object V);// Adds an object as data of a new Vertex to the array

    public void addVertex(Object[] V);// Array of Objects insertion handling

    public void printMatrix();// Displays the current adjacency matrix
}
